{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "react/no-unescaped-entities": "off",       // Disables unescaped entities rule
    "react/jsx-key": "off",                      // Disables missing key prop in iterators rule
    "@next/next/no-img-element": "off"           // Disables rule for using <img> instead of <Image />
  }
}
